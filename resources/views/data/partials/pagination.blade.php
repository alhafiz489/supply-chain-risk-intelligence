@if($paginator->hasPages())<nav class="catalog-pagination mt-4"><a class="btn btn-outline-primary {{ $paginator->onFirstPage()?'disabled':'' }}" href="{{ $paginator->previousPageUrl() ?: '#' }}">Previous</a><span class="small fw-bold">Page {{ $paginator->currentPage() }} / {{ $paginator->lastPage() }}</span><a class="btn btn-outline-primary {{ $paginator->hasMorePages()?'':'disabled' }}" href="{{ $paginator->nextPageUrl() ?: '#' }}">Next</a></nav>@endif
