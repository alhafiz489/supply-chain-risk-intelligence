@php
    $isId = app()->getLocale() === 'id';
    $config = match($type) {
        'country' => ['title' => $record->name, 'eyebrow' => 'COUNTRY INTELLIGENCE', 'back' => route('data.countries'), 'backLabel' => $isId?'Kembali ke Data Negara':'Back to Countries'],
        'port' => ['title' => $record->name, 'eyebrow' => 'PORT INTELLIGENCE', 'back' => route('data.ports'), 'backLabel' => $isId?'Kembali ke Data Pelabuhan':'Back to Ports'],
        'sentiment' => ['title' => $record->word, 'eyebrow' => 'SENTIMENT LEXICON', 'back' => route('data.sentiments'), 'backLabel' => $isId?'Kembali ke Data Sentimen':'Back to Sentiment'],
        default => ['title' => $record->title, 'eyebrow' => 'NEWS INTELLIGENCE', 'back' => route('news.index'), 'backLabel' => $isId?'Kembali ke News':'Back to News'],
    };
@endphp
<!DOCTYPE html><html lang="{{ app()->getLocale() }}"><head><title>{{ $config['title'] }} - SupplyGuard</title>@include('data.partials.head')
<style>.detail-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}.detail-item{padding:16px;border:1px solid #e1e9f2;border-radius:15px;background:#f8fbff}.detail-item small{display:block;margin-bottom:5px;color:#728399;font-size:9px;font-weight:900;letter-spacing:.08em;text-transform:uppercase}.detail-item strong{color:#102542;font-size:13px;word-break:break-word}.detail-section h2{font-size:17px}.detail-wide{grid-column:1/-1}.detail-news-image{width:100%;max-height:380px;object-fit:cover;border-radius:18px}@media(max-width:900px){.detail-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:600px){.detail-grid{grid-template-columns:1fr}}</style></head>
<body class="sg-user-sidebar-layout">@include('user.partials.sidebar')<main class="catalog-page"><a class="btn btn-sm btn-outline-primary mb-3" href="{{ $config['back'] }}">← {{ $config['backLabel'] }}</a>
<section class="catalog-hero mb-4"><small class="fw-bold opacity-75">{{ $config['eyebrow'] }}</small><div class="d-flex align-items-center gap-3 mt-2">@if($type==='country' && $record->flag_url)<img src="{{ $record->flag_url }}" alt="" style="width:64px;border-radius:9px">@endif<div><h1 class="fw-bold mb-1">{{ $config['title'] }}</h1><p class="mb-0 opacity-75">{{ $type==='country'?$record->official_name:($type==='port'?($record->country?->name ?? 'Unknown country'):($type==='sentiment'?'Lexicon record':($record->source_name ?? 'External source'))) }}</p></div></div></section>

@if($type === 'country')
<section class="catalog-panel detail-section mb-4"><h2 class="fw-bold mb-3">Identity & Geography</h2><div class="detail-grid">
@foreach(['ISO 2'=>$record->iso2,'ISO 3'=>$record->iso3,'Capital'=>$record->capital,'Region'=>$record->region,'Subregion'=>$record->subregion,'Coordinates'=>($record->latitude && $record->longitude?$record->latitude.', '.$record->longitude:null),'Sovereign'=>$record->is_sovereign?'Yes':'No','UN Member'=>$record->is_un_member?'Yes':'No','Primary Language'=>$record->primary_language_native_name ?: $record->primary_language_name] as $label=>$value)<div class="detail-item"><small>{{ $label }}</small><strong>{{ filled($value)?$value:'Unavailable' }}</strong></div>@endforeach
</div></section><section class="catalog-panel detail-section"><h2 class="fw-bold mb-3">Economy, Weather & Risk</h2><div class="detail-grid">
@foreach(['Population'=>$record->population ? number_format($record->population) : null,'GDP'=>$record->gdp_usd_billion ? '$'.number_format((float)$record->gdp_usd_billion,2).' Billion':null,'Inflation'=>$record->inflation_rate!==null?$record->inflation_rate.'%':null,'Currency'=>$record->currency_code,'Exchange Rate to IDR'=>$record->exchange_rate_to_idr,'Weather'=>$record->weather_condition,'Temperature'=>$record->temperature!==null?$record->temperature.'°C':null,'Rainfall'=>$record->rainfall_mm!==null?$record->rainfall_mm.' mm':null,'Wind Speed'=>$record->wind_speed_kmh!==null?$record->wind_speed_kmh.' km/h':null,'Ports'=>number_format($record->ports_count),'News Articles'=>number_format($record->news_count),'Stored Risk Scores'=>number_format($record->risk_scores_count),'Data Completeness'=>($record->data_completeness_percent ?? 0).'%','Latest Risk Score'=>optional($record->riskScores->first())->total_score!==null?optional($record->riskScores->first())->total_score.'/100':'Unavailable','Risk Label'=>optional($record->riskScores->first())->risk_label] as $label=>$value)<div class="detail-item"><small>{{ $label }}</small><strong>{{ filled($value)?$value:'Unavailable' }}</strong></div>@endforeach
</div></section>
@elseif($type === 'port')
<section class="catalog-panel detail-section"><h2 class="fw-bold mb-3">Port Identity & Operational Condition</h2><div class="detail-grid">@foreach(['UN/LOCODE'=>$record->unlocode,'Location Code'=>$record->location_code,'IATA Code'=>$record->iata_code,'Country'=>$record->country?->name,'City'=>$record->city,'Subdivision'=>$record->subdivision_code,'Coordinates'=>($record->latitude && $record->longitude?$record->latitude.', '.$record->longitude:null),'Function'=>$record->function_code,'Status Code'=>$record->status_code,'Congestion'=>$record->congestion_level,'Delay'=>$record->delay_days!==null?$record->delay_days.' days':null,'Data Status'=>$record->data_status,'Source'=>$record->source,'Source Version'=>$record->source_version,'Last Synchronized'=>$record->synced_at?->format('d M Y H:i'),'Remarks'=>$record->remarks] as $label=>$value)<div class="detail-item {{ $label==='Remarks'?'detail-wide':'' }}"><small>{{ $label }}</small><strong>{{ filled($value)?$value:'Unavailable' }}</strong></div>@endforeach</div></section>
@elseif($type === 'sentiment')
<section class="catalog-panel detail-section"><h2 class="fw-bold mb-3">Lexicon Information</h2><div class="detail-grid">@foreach(['Word'=>$record->word,'Sentiment Type'=>ucfirst($record->type),'Analysis Weight'=>$record->weight,'Status'=>$record->is_active?'Active':'Inactive','Created'=>$record->created_at?->format('d M Y H:i'),'Last Updated'=>$record->updated_at?->format('d M Y H:i')] as $label=>$value)<div class="detail-item"><small>{{ $label }}</small><strong>{{ $value }}</strong></div>@endforeach</div></section>
@else
<section class="catalog-panel detail-section">@if($record->image_url)<img class="detail-news-image mb-4" src="{{ $record->image_url }}" alt="">@endif<div class="detail-grid mb-4">@foreach(['Country'=>$record->country?->name ?? 'Global','Category'=>$record->category,'Sentiment'=>$record->sentiment,'Positive Score'=>$record->positive_score,'Negative Score'=>$record->negative_score,'Source'=>$record->source_name,'Language'=>$record->language,'Published'=>$record->published_at?->format('d M Y H:i') ?? $record->published_date?->format('d M Y')] as $label=>$value)<div class="detail-item"><small>{{ $label }}</small><strong>{{ filled($value)?$value:'Unavailable' }}</strong></div>@endforeach</div><h2 class="fw-bold">Summary</h2><p class="text-secondary">{{ $record->summary ?: 'Summary is unavailable.' }}</p>@if($record->matched_words)<div class="detail-item mb-3"><small>Matched Sentiment Words</small><strong>{{ collect($record->matched_words)->flatten()->filter(fn($value) => is_scalar($value))->implode(', ') }}</strong></div>@endif @if($record->url)<a class="btn btn-primary" target="_blank" rel="noopener noreferrer" href="{{ $record->url }}">Open Original Article ↗</a>@endif</section>
@endif
</main></body></html>
